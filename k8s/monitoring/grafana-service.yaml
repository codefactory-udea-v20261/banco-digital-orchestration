apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: banco-digital
  labels:
    component: monitoring
spec:
  type: ClusterIP
  selector:
    app: grafana
  ports:
    - name: web
      port: 3000
      targetPort: 3000
