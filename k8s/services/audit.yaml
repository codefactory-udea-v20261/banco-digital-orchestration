apiVersion: v1
kind: Service
metadata:
  name: audit
  namespace: banco-digital
  labels:
    app: audit
spec:
  type: ClusterIP
  selector:
    app: audit
  ports:
    - port: 8082
      targetPort: 8082
      name: http
