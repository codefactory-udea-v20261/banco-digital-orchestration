apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit
  namespace: banco-digital
  labels:
    app: audit
spec:
  replicas: 1
  selector:
    matchLabels:
      app: audit
  template:
    metadata:
      labels:
        app: audit
    spec:
      serviceAccountName: audit
      containers:
        - name: audit
          image: us-central1-docker.pkg.dev/project-2183efe1-b87d-4c12-966/banco-digital-repo/banco-digital-audit:0b2c878f023bc7bc9767e55063b5995c60aec83b
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8082
              name: http
          envFrom:
            - configMapRef:
                name: shared-config
            - configMapRef:
                name: audit-config
          env:
            - name: PORT
              value: "8082"
            - name: DB_USERNAME
              value: "audit_user"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: AUDIT_DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: JWT_SECRET
          # tcpSocket probes — Spring Security in this service blocks
          # /actuator/health/* with HTTP 403 for unauthenticated callers.
          # See identity.yaml for the same workaround.
          readinessProbe:
            tcpSocket:
              port: 8082
            initialDelaySeconds: 120
            periodSeconds: 15
            failureThreshold: 3
            timeoutSeconds: 5
          livenessProbe:
            tcpSocket:
              port: 8082
            initialDelaySeconds: 120
            periodSeconds: 30
            failureThreshold: 3
            timeoutSeconds: 5
          resources:
            requests:
              cpu: "300m"
              memory: "384Mi"
            limits:
              cpu: "750m"
              memory: "768Mi"
